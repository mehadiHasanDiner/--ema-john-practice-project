/**
 * 1. create firebase project
 * 2. create webapp
 * 3. npm install firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication > get started > enable sign in method
 * 6. create webapp sign up and login route
 * */

/**
 * 1. crate a context provider
 * 2. crate a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 * */

/**
 * HOSTING
 * --------------
 * 1. One time per computer
 * 2. npm install -g firebase-tools
 * 3. firebase login
 *
 *
 *
 * For each project one time
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down arrow) use space bar to select
 * 4. existing project
 * 5. select the project carefully
 * 6. which project as public directory:
 *  ---dist (vite users) ---build (create react app users)
 * single pate deployment: yes.
 *
 *
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 * */
